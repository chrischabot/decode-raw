{
  "name": "raw-decoder",
  "version": "1.0.0",
  "description": "CLI tool to decode Ethereum transaction raw strings",
  "main": "dist/index.js",
  "bin": {
    "decode-raw": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["ethereum", "transaction", "decoder", "cli"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ethers": "^6.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0"
  }
}
